# XVC Server for USB-to-JTAG FTDI chips

Platform-independent XVC (Xilinx Virtual Cable) server implementation for connecting to an AMD device via a USB-to-JTAG FTDI bridge

## Usage

This crate provides a command-line server binary. See `xvc-ftdi --help` for available options.
